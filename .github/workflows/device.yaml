name: C/C++ CI

on:
  push:
    branches: [ "nemirko" ]
  pull_request:
    branches: [ "nemirko" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
    - name: execute
      run: ./program.out
