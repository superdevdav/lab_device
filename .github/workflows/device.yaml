name: C/C++ CI

on:
  push:
    branches: [ "dusalin" ]
  pull_request:
    branches: [ "dusalin" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
    - name: execute
      run: ./a.out
